# Docker-compatible requirements file
# This file excludes packages that are difficult to build in Docker environments

# Core NeMo and PyTorch dependencies
nemo-toolkit[nlp]>=1.20.0
torch>=2.0.0
torchvision
torchaudio
pytorch-lightning>=2.0.0

# Data processing
pyyaml>=6.0
pandas>=1.5.0
numpy>=1.24.0
datasets>=2.14.0

# Model and tokenization
transformers>=4.30.0
tokenizers>=0.13.0
sentencepiece>=0.1.99

# LoRA and PEFT
peft>=0.4.0

# Evaluation and metrics
evaluate>=0.4.0
rouge-score>=0.1.2
bleu>=0.2.0
sacrebleu>=2.3.0

# Utilities
tqdm>=4.65.0
wandb>=0.15.0
tensorboard>=2.13.0
omegaconf>=2.3.0
hydra-core>=1.3.0

# Development and testing
pytest>=7.4.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.5.0

# Note: apex and flash-attn are excluded from automatic installation
# due to compilation complexity in Docker environments.
# Use the provided scripts to install them if needed:
# - ./scripts/install_apex.sh
# - ./scripts/install_flash_attn.sh
