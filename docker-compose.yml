version: '3.8'

services:
  codellama-finetune:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: codellama-finetune

    # GPU support - works without host CUDA installation
    # The container includes its own CUDA toolkit
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu, compute, utility]

    # Environment variables
    environment:
      - CUDA_VISIBLE_DEVICES=all  # Use all available GPUs
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=compute,utility
      - HF_HOME=/workspace/cache
      - TRANSFORMERS_CACHE=/workspace/cache
      - HF_DATASETS_CACHE=/workspace/cache
      - TORCH_HOME=/workspace/cache
      - WANDB_DISABLED=true  # Disable wandb by default
      - CUDA_LAUNCH_BLOCKING=1  # For debugging CUDA issues
      - PYTHONPATH=/workspace/src
      - FORCE_CUDA=1  # Force CUDA compilation if needed
    
    # Volume mounts
    volumes:
      # Mount output directory to persist trained models
      - ./output:/workspace/output

      # Mount data directory for training data
      - ./data:/workspace/data

      # Mount configs for easy modification
      - ./configs:/workspace/configs

      # Mount cache directory to persist downloaded models
      - model_cache:/workspace/cache

      # Mount logs directory
      - training_logs:/workspace/logs

      # Mount scripts for easy modification during development
      - ./scripts:/workspace/scripts

    # Resource limits and reservations
    deploy:
      resources:
        limits:
          memory: 32G  # Adjust based on your system
        reservations:
          memory: 8G
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu, compute, utility]

    # Shared memory size for PyTorch DataLoader
    shm_size: '2gb'

    # Working directory
    working_dir: /workspace

    # Keep container running
    tty: true
    stdin_open: true

    # Restart policy
    restart: unless-stopped

    # Health check - works without requiring GPU
    healthcheck:
      test: ["CMD", "python", "-c", "import torch, transformers, peft; print('Container healthy')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s



# Named volumes for persistence
volumes:
  model_cache:
    driver: local
  training_logs:
    driver: local

# Networks
networks:
  default:
    name: codellama-network
