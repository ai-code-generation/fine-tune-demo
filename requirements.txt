# NeMo 24.07 Fine-tuning Pipeline Requirements
# These are the essential dependencies for the pipeline

# Core dependencies (usually pre-installed in NeMo container)
torch>=2.0.0
transformers>=4.30.0
datasets>=2.10.0
huggingface-hub>=0.16.0

# Data processing
pyyaml>=6.0
numpy>=1.21.0

# Optional: For local development outside container
# nemo-toolkit[all]>=1.20.0
